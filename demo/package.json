{
  "name": "demo",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^17.0.2",
    "t-frame": "file:../core/build",
    "react-dom": "^17.0.2",
    "react-scripts": "5.0.0",
    "web-vitals": "^2.1.0"
  },
  "scripts": {
    "dev": "PORT=3100 react-scripts start",
    "build": "react-scripts build",
    "eject": "react-scripts eject",
    "load-module": "cp -r ../core/build node_modules/t-frame/",
    "reload-module": "rm -rf node_modules/t-frame && yarn load-module"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
